var regist = require('regist');

module.exports = function(req, res){
  regist.key.forEach(function(element){
    if(element['KEY']===req.cookies.key){
      var cursor = Db.db.collection('USERS').find({email: element['email']});
      cursor.toArray(function(err, docs){
        if(err) throw err;
        if(docs.length===0)
          res.end(JSON.stringify({message: 'No such token'}));
        var user_obj = docs[0];
        res.json({user_obj['email'], user_obj['nickname']});
      });
    }
  )};
}
